@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@*@model IEnumerable<OnlineShop.Models.ProductTypes>  //এই View() তে ProductTypes এর একটা collection আসবে।।*@ 

@using OnlineShop.Models
@model  IEnumerable<ProductTypes>

@{ 
    ViewData["Title"] = "Index";
}

<br /><br />
<div class="row">
    <div class="column-6">
        <h2 class="text-info">Product type List</h2>
    </div>
    <div class="column-6 text-right">
        <a asp-action="Create" class="btn btn-info">&nbsp; New Product Type</a>
    </div>
</div>

<br />

<div>
    <table class="table-info">
        <tr>
            <th>
                @Html.DisplayNameFor(c=>c.ProductType)
            </th>
        </tr>
        @foreach(var item in Model)
            {
                <tr>
                    <td>@item.ProductType</td>
                    <td>Edit | Details | Delete</td>
                </tr>
            }
    </table>
</div>